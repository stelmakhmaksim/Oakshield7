package com.dms.lab7.repository;

public interface TrajectoryRep /*extends JpaRepository<Trajectory, Long>*/ {
    //@Query(value = "select * from DECISION", nativeQuery = true)
    //List<Trajectory> findAllByProcessId(Long processId);
}
